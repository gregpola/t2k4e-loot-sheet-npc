<header class="sheet-header">
    <div class="sheet-profile-img">
        <img class="sheet-profile" src="{{actor.img}}" title="{{actor.name}}" data-edit="img" />
    </div>
    <h1 class="charname">
        {{#if isGM}}
            <input name="name" type="text" value="{{actor.name}}" placeholder="{{localize " Character Name"}}" />
        {{else}}
            {{actor.name}}
        {{/if}}
    </h1>
</header>
